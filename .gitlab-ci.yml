variables:
  VERSION_ID: '0.9.1-$CI_COMMIT_SHORT_SHA'

stages:
  - build

build and publish installer:
  image: electronuserland/builder:wine
  stage: build
  script:
    - sed -i "s/0.0.0/${VERSION_ID}/g" package.json
    - npm install && npm run publish