variables:
  VUE_APP_VERSION_ID: '0.9.2-$CI_COMMIT_SHORT_SHA'

stages:
  - build

build and publish installer:
  image: electronuserland/builder:wine
  stage: build
  script:
    - sed -i "s/APP_VER/${VUE_APP_VERSION_ID}/g" package.json
    - npm install && npm run publish